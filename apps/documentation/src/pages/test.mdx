---
title: test
route: /test
hide: true
---

import { Checkbox, Fieldset, CheckboxPanel, TextField, RadioPanel, RadioGroup } from '@entur/form';
import { MultiSelect } from '@entur/dropdown';
import {
  FILTERS,
  dropdownItems,
  countries,
  cities,
  fuzzyMatch,
} from '@data/dropdownItems-data.ts';


# Fokus state test

```jsx
    <Heading2>Skjemaelementer</Heading2>
        <TextField size="medium" label="Skriv her" variant="information"></TextField>
        <br/>
        <TextField size="medium" label="Skriv her" variant="error"></TextField>
        <br/>
        <TextField size="medium" label="Skriv her" labelTooltip="test"></TextField>
        <br/>
        <TextField size="medium" label="Skriv her" variant="success"></TextField>
        <br/>
        <div
        style={{
            display: 'flex',
            flexDirection: 'column',
            gap: '0.5rem',
            width: '20rem',
        }}
        >
        <CheckboxPanel title="Setereservasjon" value="seat" />
        <CheckboxPanel title="Setereservasjon" value="seat" />
        </div>
    <div style={{ display: 'flex', justifyContent: 'center' }}>
    <Fieldset label="Hvilke sparkesykler vil du se i kartet?">
    <Checkbox>Tier</Checkbox>
    <Checkbox>Voi</Checkbox>
    <Checkbox>Zvipp</Checkbox>
    </Fieldset>
    </div>
<Button width="auto" variant="primary" size="medium">Send</Button>
```

```jsx
() => {
    const [value, setValue] = React.useState(null);
    return (
      <div style={{ width: '20rem' }}>
        <RadioGroup
          name="ticket-type"
          label="Velg billettype:"
          onChange={e => setValue(e.target.value)}
          value={value}
        >
          <div style={{ display: 'grid', gridGap: '0.5rem' }}>
            <RadioPanel
              title="Standard"
              secondaryLabel="199,-"
              value="non-flexi"
              size="large"
            >
              Ikke fleksibel
            </RadioPanel>
            <RadioPanel
              title="Premium"
              secondaryLabel="299,-"
              value="flexi"
              size="large"
            >
              <div>Fleksibel billett</div>
              {value === 'flexi' && (
                <Label as="div">
                  Billetten kan refunderes eller endres ved et senere tidspunkt
                </Label>
              )}
            </RadioPanel>
          </div>
        </RadioGroup>
      </div>
    );
  }
```

```jsx
  () => {
    const [value, setValue] = React.useState(null);
    return (
      <div style={{ margin: '5% 0' }}>
      <RadioGroup
        name="ticket-type"
        label="Velg billettype"
        onChange={e => setValue(e.target.value)}
        value={value}
      >
        <Radio value="single">Enkelbillett</Radio>
        <Radio value="period">Periodebillett</Radio>
      </RadioGroup>
      </div>
    );
  }
```

<Playground style={{ overflow: 'visible', margin: 'auto', width: '50%' }}
code={`() => {
    const [selectedCities, setSelectedCities] = React.useState([]);
    return (
      <MultiSelect
        label="Velg fine byer"
        items={cities}
        selectedItems={selectedCities}
        onChange={setSelectedCities}
      />
    );
  }`}
scope={{cities}} />
