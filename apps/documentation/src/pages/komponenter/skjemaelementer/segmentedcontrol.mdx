---
title: SegmentedControl
description: SegmentedControl lar brukeren velge mellom nært beslektede alternativer som påvirker innhold eller visning. Den består av en gruppe knapper hvor nøyaktig ett alternativ kan være valgt om gangen.
route: /komponenter/skjemaelementer/segmentedcontrol
parent: Komponenter
menu: Skjemaelementer
npmPackage: form
tags: valg, filtrering, visningsbytte, togglegroup, toggle
---

import { now } from '@internationalized/date';
import { SegmentedControl, SegmentedChoice, TextField } from '@entur/form';
import { DatePicker, TimePicker } from '@entur/datepicker';
import { Dropdown } from '@entur/dropdown';
import { AlignLeftIcon, AlignCenterIcon, AlignRightIcon } from '@entur/icons';
import { Tooltip } from '@entur/tooltip';
import { SEO } from '@components/seo/SEO';

export const Head = ( props ) => {
  return <SEO title={props.pageContext.frontmatter.title} description={props.pageContext.frontmatter.description} pathname={props.pageContext.frontmatter.route} />
}

<PageHeader />

```jsx
() => {
  const [travelTime, setTravelTime] = React.useState("na");
  return (
    <div style={{ width: '55%', margin: '1rem auto' }}>
      <SegmentedControl
        label="Velg reisetidspunkt"
        value={travelTime}
        onChange={setTravelTime}
      >
        <SegmentedControl.Item value="na">Nå</SegmentedControl.Item>
        <SegmentedControl.Item value="avreise">Avreise</SegmentedControl.Item>
        <SegmentedControl.Item value="ankomst">Ankomst</SegmentedControl.Item>
      </SegmentedControl>
    </div>
  );
}
```

## Oversikt

### SegmentedControl

`SegmentedControl` grupperer to eller flere valg og lar brukeren velge ett alternativ. Den kan brukes kontrollert med `value` eller ukontrollert med `defaultValue`.

### SegmentedControl.Item og SegmentedChoice

Hvert valg angis med `SegmentedControl.Item` (foretrukket) eller `SegmentedChoice`. Send inn en unik `value` som identifiserer valget, og bruk tekst og/eller ikon som innhold.


<SmallAlertBox variant="warning" title="Endring i API">
  `selectedValue` er avviklet. Bruk `value` (kontrollert) eller `defaultValue` (ukontrollert) i stedet.
</SmallAlertBox>

## Retningslinjer

- **Egnet til**: filtrering av innhold og veksling mellom visninger/innstillinger. Passer godt for valg som påvirker nærliggende innhold (f.eks. "Avreise" vs. "Ankomst").
- **Uegnet til**: navigasjon mellom paneler/sider (bruk `Tabs`), og som erstatning for `RadioGroup` i skjema med innsending.
- **Plassering**: plasser komponenten nær innholdet den styrer. Bruk `aria-controls` hvis den styrer innhold lenger nede på siden.
- **Antall og bredde**: hold antall valg lavt (≤ 5). Sørg for at alle valg er synlige på skjermen. Hold tekstene korte og konsistente.
- **Ikoner**: kun ikoner kan brukes for ekspertbrukere. Gi alltid en skjermleser-tekst med `aria-label` når innholdet ikke er selvdokumenterende.
- **Label**: bruk `label` for å beskrive hva valget styrer for å gjøre det lettere for brukeren å forstå der det er nødvendig.

Anbefalingene er inspirert av bruksmønstre i ToggleGroup i [Aksel](https://aksel.nav.no/komponenter/core/toggle-group) og [Designsystemet](https://storybook.designsystemet.no/?path=/docs/komponenter-togglegroup--docs).

## Eksempler

### Endret visning avhengig av valg

<Playground code={`() => {
    const [type, setType] = React.useState("avreise");
    const [dateTime, setDateTime] = React.useState(now('Europe/Oslo'));
    return (
      <GridContainer
        spacing="medium"
        style={{ marginBottom: '1rem', width: '65%' }}
      >
        <GridItem small={12}>
          <SegmentedControl
            label="Velg reisetidspunkt"
            value={type}
            onChange={setType}
          >
            <SegmentedControl.Item value="nå">Nå</SegmentedControl.Item>
            <SegmentedControl.Item value="avreise">Avreise</SegmentedControl.Item>
            <SegmentedControl.Item value="ankomst">Ankomst</SegmentedControl.Item>
          </SegmentedControl>
        </GridItem>
        {type !== "nå" && 
          <GridItem small={12} style={{ display: 'flex', gap: '1rem' }}>
            <DatePicker
              label={type === "avreise" ? "Avreisedato" : "Ankomstdato"}
              selectedDate={dateTime}
              onChange={date => setDateTime(date)}
            />
            <TimePicker
              label="Tidspunkt"
              selectedTime={dateTime}
              onChange={time => setDateTime(time)}
            />
          </GridItem> 
        }
      </GridContainer>
    );
  }`} scope={{now}} />

### Ukontrollert med defaultValue

```jsx
() => (
  <SegmentedControl label="Visning" defaultValue="liste">
    <SegmentedControl.Item value="liste">Liste</SegmentedControl.Item>
    <SegmentedControl.Item value="graf">Graf</SegmentedControl.Item>
    <SegmentedControl.Item value="tabell">Tabell</SegmentedControl.Item>
  </SegmentedControl>
)
```

### Kun ikoner med skjermleser-tekst

```jsx
() => (
  <SegmentedControl label="Tekstjustering" defaultValue="left">
    <Tooltip content="Venstrejuster">
      <SegmentedControl.Item value="left" aria-label="Venstrejuster">
        <AlignLeftIcon aria-hidden />
      </SegmentedControl.Item>
    </Tooltip>
    <Tooltip content="Midtstill">
      <SegmentedControl.Item value="center" aria-label="Midtstill">
        <AlignCenterIcon aria-hidden />
      </SegmentedControl.Item>
    </Tooltip>
    <Tooltip content="Høyrejuster">
      <SegmentedControl.Item value="right" aria-label="Høyrejuster">
        <AlignRightIcon aria-hidden />
      </SegmentedControl.Item>
    </Tooltip>
  </SegmentedControl>
)
```

## Tilgjengelighet

- `SegmentedControl` har `role="radiogroup"` og hvert valg har `role="radio"` med `aria-checked` for valgt tilstand.
- Roving tabindex: piltaster flytter fokus mellom valg. `Space`/`Enter` velger, `Escape` fjerner fokus.
- Bruk `label` for å beskrive hva valgene styrer, og `aria-label` på valg som kun har ikon.
- Knytt gjerne komponenten til innholdet den styrer med `aria-controls` på `SegmentedControl` når relevant.

<BannerAlertBox variant="information" title="Husk beskrivelser for skjermlesere">
  Om du sender inn ikoner eller tekster som krever en visuell kontekst, er det
  viktig at du sender inn en <CodeText>aria-label</CodeText> prop, eller bruker{' '}
  <CodeText>VisuallyHidden</CodeText>
  (fra <CodeText>@entur/a11y</CodeText>) for å gi valget en god beskrivelse.
</BannerAlertBox>


## Props

### SegmentedControl

<Props componentName="SegmentedControl" npmPackage={props.pageContext.frontmatter.npmPackage}/>

### SegmentedChoice

<Props componentName="SegmentedChoice" npmPackage={props.pageContext.frontmatter.npmPackage}/>

