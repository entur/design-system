---
title: RangeCalendar
description: RangeCalendar er en inline-kalenderkomponent for å velge en datoperiode (fra- og til-dato).
route: /komponenter/skjemaelementer/rangecalendar
parent: Komponenter
menu: Skjemaelementer
npmPackage: datepicker
tags: dato, date, kalender, calendar, periode, range, fra, til, velger
---

import { now, today, isWeekend } from '@internationalized/date';

import {
  RangeCalendar,
} from '@entur/datepicker';

import { SEO } from '@components/seo/SEO';

import './styles.scss';

export const Head = ( props ) => {
  return <SEO title={props.pageContext.frontmatter.title} description={props.pageContext.frontmatter.description} pathname={props.pageContext.frontmatter.route} />
}


<Playground
  hideContrastOption
  code={`() => {
    const [range, setRange] = React.useState(null);
    return (
      <RangeCalendar
        value={range}
        onChange={setRange}
        locale="nb-NO"
      />
    );
  }`}
  scope={{RangeCalendar}}
/>

## Kom i gang

RangeCalendar lar brukeren velge en datoperiode (fra- og til-dato) i en inline-kalender. Den fungerer med samme type datoobjekter som [Calendar](/komponenter/skjemaelementer/calendar) og støtter de fleste av de samme props-ene, men tar inn og returnerer et objekt med `start` og `end` i stedet for én enkelt dato.

For enkeltdatovelger, se [Calendar](/komponenter/skjemaelementer/calendar). Props som er spesifikke for enkeltdatovelgeren (`onSelectedCellClick`, `onCellClick` og `forcedReturnType`) er ikke tilgjengelige.

<SmallAlertBox variant="information">
  <div>
    <strong>OBS:</strong> hjelpefunksjoner fra{' '}
    <CodeText>@internationalized/date</CodeText> (typ.{' '}
    <CodeText>now()</CodeText> og <CodeText>isWeekend()</CodeText>) er ikke
    inkludert i <CodeText>@entur/datepicker</CodeText>, legg til{' '}
    <CodeText>@internationalized/date</CodeText> i repo ditt for å bruke dem.
  </div>
</SmallAlertBox>

### Språk og locale

Språk og locals er støttet gjennom prop-en `locale` eller [react-aria](https://react-spectrum.adobe.com/react-aria/I18nProvider.html) sin `<I18nProvider />`.
`locale` støtter strenger på BCP 47-formatet, eks. `nb-NO` for norsk. Her finner du en [liste over BCP 47-koder](https://www.techonthenet.com/js/language_tags.php). Som default velges den locale-en som er satt på brukeren maskin.

`navigationDescription` er en prop som forteller brukeren hvordan de kan navigere i kalenderen med tastaturet. En norsk og engelsk verdi følger med automatisk, men andre språk må legges inn selv.

### Vise flere måneder samtidig

Bruk `visibleDuration`-prop-en for å vise flere måneder samtidig i periodekalenderen.

<Playground hideContrastOption code={`() => {
    const [range, setRange] = React.useState(null);
    return (
      <RangeCalendar
        value={range}
        onChange={setRange}
        locale="nb-NO"
        visibleDuration={{ months: 2 }}
      />
    );
  }`}
  scope={{RangeCalendar}} />

### Datovalidering

Hvis du ønsker å begrense tilgjengelige datoer kan du benytte `minDate`- og `maxDate`-props-ene eller `isDateUnavailable()`-valideringsfunksjonen. `minDate` og `maxDate` tar inn et `CalendarDate`-objekt. `isDateUnavailable()` tar inn en `DateValue` og returnerer en `boolean`.

<Playground
  defaultShowEditor
  hideContrastOption
  code={`() => {
    const [range, setRange] = React.useState(null);
    return (
      <RangeCalendar
        value={range}
        onChange={setRange}
        locale="nb-NO"
        minDate={today('Europe/Oslo')}
        maxDate={today('Europe/Oslo').add({ months: 2 })}
      />
    );
  }`}
  scope={{RangeCalendar, today}}
/>

### Styling av datoer

RangeCalendar har støtte for prop-en `classNameForDate`. `classNameForDate` skal være en funksjon som tar inn en dato og returnerer en streng med klassenavnet som skal legges til for dato-ruten.

Hvis stylingen som legges til er meningsbærende bør du også bruke `ariaLabelForDate` til å beskrive stylingens mening for skjermlesere o.l.

<Playground hideContrastOption
code={`() => {
    const [range, setRange] = React.useState(null);
    return (
      <RangeCalendar
        value={range}
        onChange={setRange}
        locale="nb-NO"
        classNameForDate={date => isWeekend(date, 'nb-NO') ? 'special-class' : ''}
        ariaLabelForDate={date => isWeekend(date, 'nb-NO') ? ', helgedag' : ''}
      />
    );
  }`}
scope={{RangeCalendar, isWeekend}} />

### Vise ukenummer

Hvis du trenger å vise ukenummer i kalenderen kan du benytte prop-en `showWeekNumbers`. Du kan endre overskriften til ukenummerkolonnen ved å bruke prop-en `weekNumberHeading`.

<Playground hideContrastOption
code={`() => {
    const [range, setRange] = React.useState(null);
    return (
      <RangeCalendar
        value={range}
        onChange={setRange}
        locale="nb-NO"
        showWeekNumbers
      />
    );
  }`}
scope={{RangeCalendar}} />

## Retningslinjer

### Universell utforming

RangeCalendar bruker [react-aria](https://react-spectrum.adobe.com/react-aria/useRangeCalendar.html) i bakgrunnen. Denne pakken sørger for gjennomgående støtte for universell utforming.
Alle interagerbare elementer i kalenderen har aria-beskrivelser og er tilgjengelige med tastaturet.

Hvis du endrer `locale` (dvs. språk) til noe annet enn norsk og engelsk må du sende inn verdier på riktig språk til `navigationDescription`-prop-en.

## Komponenter

### RangeCalendar

<ImportStatement imports="RangeCalendar" packageName={props.pageContext.frontmatter.npmPackage} />
<Props componentName="RangeCalendar" />
