---
title: Aktørkart
description: Et verktøy for å kartlegge aktører som blir berørt i et prosjekt
route: /ressurser/aktorkart
parent: ressurser
menu: Workshopmaler
order: 2
---

import { graphql } from 'gatsby';
import { SEO } from '@components/seo/SEO';
import { DetailsList }  from '@components/Pages/DetailsList';
import { TimeglassIcon, UsersIcon, ClockIcon } from '@entur/icons';
import { NavigationCard } from '@entur/layout';
export const Head = ( props ) => {
  return <SEO title={props.pageContext.frontmatter.title} description={props.pageContext.frontmatter.description} pathname={props.pageContext.frontmatter.route} />
}

export const query = graphql`
query fargerQuery {
  files: allFile(filter: {sourceInstanceName: {eq: "media"}, relativeDirectory:  {glob: "images/ressurser/**"}, extension: {eq: "png"}}) {
    images: nodes {
      name
       publicURL
       childImageSharp {
         gatsbyImageData(
          layout: CONSTRAINED
        )
      }
    }
  }
}
`;

<PageHeader/>

<DetailsList
  items={[
    { icon: <TimeglassIcon />, label: 'Tidsbruk', description: '60-120 min' },
    { icon: <UsersIcon />, label: 'Deltakere', description: '5-10' },
    { icon: <ClockIcon />, label: 'Når', description: 'Både før og underveis i et utviklingsarbeid' },
  ]}
/>

<ImageDisplay
  imgSource={props.data.files.images.find(image => image.name === "aktorkart").childImageSharp.gatsbyImageData}
  downloadSources={[
    { src: props.data.files.images.find(image => image.name === "aktorkart").publicURL, format: "png", label: "Last ned aktørbilde i png" }
  ]}
  name={props.data.files.images.find(image => image.name === "aktorkart").name}
  preset="full-width-image"
  alt=""
/>

<GridContainer spacing="medium">
<GridItem small={12} medium={6}>
<NavigationCard title="Figma" titleIcon={<ClockIcon/>} href="" compact></NavigationCard>
</GridItem>
<GridItem small={12} medium={6}>
<NavigationCard title="Mural" titleIcon={<ClockIcon/>} href="" compact></NavigationCard>
</GridItem>
</GridContainer>
