---
title: Farger
description: Fargepaletten er et viktig grafisk virkemiddel i identiteten vår. Det er viktig at fargene brukes riktig og med omhu, for å oppnå et gjenkjennelig uttrykk.
route: /identitet/verktoykassen/farger
parent: Identitet
menu: Verktøykassen
order: 4
---

import { graphql } from 'gatsby'
import { SEO } from '@components/seo/SEO';
import { borderWidths, colors, space } from '@entur/tokens';
import ColorCardList from '@components/Colors/ColorCardList';
import './farger.scss';

export const Head = ( props ) => {
  return <SEO title={props.pageContext.frontmatter.title} description={props.pageContext.frontmatter.description} pathname={props.pageContext.frontmatter.route} />
}

export const query = graphql`
query fargerQuery {
  files: allFile(filter: {sourceInstanceName: {eq: "media"}, relativeDirectory:  {glob: "images/identitet/verktoykassen/farger"}, extension: {eq: "png"}}) {
    images: nodes {
      name
       childImageSharp {
         gatsbyImageData(
          layout: CONSTRAINED
        )
      }
    }
  }
}
`;

<PageHeader/>

Fargene har gode kontraster som til sammen gir et lekent og livlig uttrykk. I tillegg symboliserer fargene noen av verdiene vi står for.

## Fargeoversikt

Når det er snakk om kjernekommunikasjon, skal vi prøve å forholde oss til disse hovedfargene.

### Primærfarger

Primærfargene er de mest sentrale fargene i Entur.

<ColorCardList colorType="primary" />

### Sekundærfarger

Sekundærpaletten brukes primært dekorativt og tilfører visuell dybde.

<ColorCardList colorType="secondary" />

## Fargebruk

### For print

På trykte flater skal merkevaren vår være **tydelig og lett gjenkjennelig**. Vi skal fremstå som profesjonelle, men kan gjerne være lekne i uttrykket der det passer.

**Kjernefargene skal alltid være med** i designet – de bygger identitet og styrker gjenkjennelsen, uansett format. Sekundærfargene brukes som støtte for å skape dybde og variasjon.

I trykk har vi mer rom for å eksperimentere med farger, former og typografi, og skape energi og visuell interesse. Vi bruker også tekst som et grafisk virkemiddel, med ulike farger og vekter for å fremheve budskap.

### For digitale flater

For digitale flate holder et **rent og enkelt uttrykk**, der blått og hvitt dominerer. Korall brukes som støtteelement for å forsterke merkevaren, og sekundærfargene støtter opp om enkelte designelementer for å skape dybde og variasjon.

Her har vi utvidet fargepaletten med flere nyanser av kjerne- og sekundærfargene, samt egne statusfarger og transportsfarger. Dette gir større fleksibilitet og gjør det mulig å tilpasse designet etter funksjon og de forventningene man har til digitale grensesnitt.

Sammenlignet med trykk, skal det digitale uttrykket være mer nedtonet – her er det innholdet som skal være i fokus. Designet skal gjøre det enkelt å bruke og forstå, uten å konkurrere med budskapet.

### Vekting av farge

Hvordan fargene vektes er en viktig del av å skape det ønskede uttrykket for Entur. Hovedfargene er (blue), (white) og (coral), og disse burde alltid være med. Korallfargen er vektet lavt, men er viktig og skal alltid være tilstede i uttak. Den brukes ofte til detaljer, til tider så detaljert som bare i logo.

<ImageDisplay
  imgSource={
    props.data.files.images.find(image => image.name === 'vekting')
      ?.childImageSharp.gatsbyImageData
  }
  alt="Viser vektleggingen av profilfargene, Blue og white er størst, så lavender, coral og til slutt peach"
  preset="full-width-image"
/>

### Eksempler

<div className="identitet-farger__eksempler">

<ImageDisplay
  imgSource={
    props.data.files.images.find(image => image.name === 'plakat')
      ?.childImageSharp.gatsbyImageData
  }
  alt="Viser eksempel på vekting på plakat"
  preset="full-width-image"
/>
<Paragraph>Entur verdi plakat</Paragraph>


<ImageDisplay
  imgSource={
    props.data.files.images.find(image => image.name === 'web')
      ?.childImageSharp.gatsbyImageData
  }
  alt="Viser eksempel på vekting på web"
  preset="full-width-image"
/>
<Paragraph>Entur app</Paragraph>

<ImageDisplay
  imgSource={
    props.data.files.images.find(image => image.name === 'interneapp')
      ?.childImageSharp.gatsbyImageData
  }
  alt="Viser eksempel på vekting på web"
  preset="full-width-image"
/>
<Paragraph>Entur interne applikasjoner</Paragraph>

<ImageDisplay
  imgSource={
    props.data.files.images.find(image => image.name === 'mobil')
      ?.childImageSharp.gatsbyImageData
  }
  alt="Viser eksempel på vekting på mobil"
  preset="full-width-image"
/>

<Paragraph>Entur web</Paragraph>
</div>

### Unngå..

 <GridContainer className="identitet-farger__fargenyanser" spacing="large">
    <GridItem small={12} medium={6} large={6}>
      <DoDontCard
        contrast
        variant="negative"
        imgSource={
          props.data.files.images.find(image => image.name === 'blue')
            .childImageSharp.gatsbyImageData
        }
        alt=""
        noPadding
      >
        <Paragraph>Mørk tekst på blå bakgrunn</Paragraph>
      </DoDontCard>
    </GridItem>
    <GridItem small={12} medium={6} large={6}>
      <DoDontCard
        contrast
        variant="negative"
        imgSource={
          props.data.files.images.find(image => image.name === 'coral')
            .childImageSharp.gatsbyImageData
        }
        alt=""
        noPadding
      >
        <Paragraph>Korall som bakgrunn</Paragraph>
      </DoDontCard>
    </GridItem>
    <GridItem small={12} medium={6} large={6}>
      <DoDontCard
        contrast
        variant="negative"
        imgSource={
          props.data.files.images.find(image => image.name === 'grey')
            .childImageSharp.gatsbyImageData
        }
        alt=""
        noPadding
      >
        <Paragraph>
          Kombinasjoner som ikke er identitetsbærende
        </Paragraph>
      </DoDontCard>
    </GridItem>
    <GridItem small={12} medium={6} large={6}>
      <DoDontCard
        contrast
        variant="negative"
        imgSource={
          props.data.files.images.find(image => image.name === 'white')
            .childImageSharp.gatsbyImageData
        }
        alt=""
        noPadding
      >
        <Paragraph>Peach farge på lys bakgrunn</Paragraph>
      </DoDontCard>
    </GridItem>
</GridContainer>

## Universell utforming

Farge er avgjørende for god lesbarhet, uansett hvor og hvordan designet brukes – enten det er i animasjon, print, datavisualisering eller brukergrensesnitt. Hvis fargene ikke har tilstrekkelig kontrast i lysstyrke eller nyanse, kan det bli vanskelig for svaksynte å skille mellom tekst og grafiske elementer. Sørg for at fargevalgene dine støtter både budskapet og brukeropplevelsen – ikke svekker dem.

### Kontrast ratio

Når du bruker farger på tekst eller elementer mot en bakgrunn, må du passe på at det er god nok kontrast for at innholdet skal være lett å lese.

Kontrastkravene er:
- Tekst: minst 4.5:1
- Ved normal teksttykkelse er stor tekst 18 punkt og større.
- Ved fet tekststil er stor tekst 14 punkt og større.

Derfor er det viktig å teste med en [kontrastsjekker](/universell-utforming/kontrast-sjekker/) for å sikre at kombinasjonen oppfyller kravene.

Les mer om kontrastkrav for farger på [Universell utforming](/universell-utforming) siden og bruk vår [Color Contrast Checker](/universell-utforming/kontrast-sjekker) for å hjelpe deg med å bestemme lesbarheten til tekst og kontrasten til grafiske objekter.

## Fargenyanser

I Entur bruker vi fargenyanser både i oppbyggingen av komponenter og i visualiseringer eller workshopmateriale. For å se hele spekteret av fargekoder, gå til [tokens](/tokens/fargetokens/oversikt-farger).

 <GridContainer
   className="identitet-farger__fargenyanser"
   spacing="large"
 >
   <GridItem small={12} medium={6} large={4}>
   <ImageDisplay
  imgSource={
    props.data.files.images.find(image => image.name === 'peach-nyanse')
      ?.childImageSharp.gatsbyImageData
  }
  className="identitet-farger__fargenyanser-image"
  alt="Viser eksempel på nyanser av peach"
  preset="full-width-image"
/>
    </GridItem>
       <GridItem small={12} medium={6} large={4}>
   <ImageDisplay
  imgSource={
    props.data.files.images.find(image => image.name === 'coral-nyanse')
      ?.childImageSharp.gatsbyImageData
  }
  className="identitet-farger__fargenyanser-image"
  alt="Viser eksempel på nyanser av coral"
  preset="full-width-image"
/>
    </GridItem>
       <GridItem small={12} medium={6} large={4}>
   <ImageDisplay
  imgSource={
    props.data.files.images.find(image => image.name === 'canery-nyanse')
      ?.childImageSharp.gatsbyImageData
  }
  className="identitet-farger__fargenyanser-image"
  alt="Viser eksempel på nyanser av canery"
  preset="full-width-image"
/>
    </GridItem>
       <GridItem small={12} medium={6} large={4}>
   <ImageDisplay
  imgSource={
    props.data.files.images.find(image => image.name === 'lavender-nyanse')
      ?.childImageSharp.gatsbyImageData
  }
  className="identitet-farger__fargenyanser-image"
  alt="Viser eksempel på nyanser av lavender"
  preset="full-width-image"
/>
    </GridItem>
       <GridItem small={12} medium={6} large={4}>
   <ImageDisplay
  imgSource={
    props.data.files.images.find(image => image.name === 'sky-nyanse')
      ?.childImageSharp.gatsbyImageData
  }
  className="identitet-farger__fargenyanser-image"
  alt="Viser eksempel på nyanser av sky"
  preset="full-width-image"
/>
    </GridItem>
       <GridItem small={12} medium={6} large={4}>
   <ImageDisplay
  imgSource={
    props.data.files.images.find(image => image.name === 'mint-nyanse')
      ?.childImageSharp.gatsbyImageData
  }
  className="identitet-farger__fargenyanser-image"
  alt="Viser eksempel på nyanser av mint"
  preset="full-width-image"
/>
    </GridItem>
       <GridItem small={12} medium={6} large={4}>
   <ImageDisplay
  imgSource={
    props.data.files.images.find(image => image.name === 'grey-nyanse')
      ?.childImageSharp.gatsbyImageData
  }
  className="identitet-farger__fargenyanser-image"
  alt="Viser eksempel på nyanser av grey"
  preset="full-width-image"
/>
    </GridItem>
       <GridItem small={12} medium={6} large={4}>
   <ImageDisplay
  imgSource={
    props.data.files.images.find(image => image.name === 'blue-nyanse')
      ?.childImageSharp.gatsbyImageData
  }
  className="identitet-farger__fargenyanser-image"
  alt="Viser eksempel på nyanser av blue"
  preset="full-width-image"
/>
    </GridItem>
       <GridItem small={12} medium={6} large={4}>
   <ImageDisplay
  imgSource={
    props.data.files.images.find(image => image.name === 'ebony-nyanse')
      ?.childImageSharp.gatsbyImageData
  }
  className="identitet-farger__fargenyanser-image"
  alt="Viser eksempel på nyanser av ebony"
  preset="full-width-image"
/>
    </GridItem>
</GridContainer>
