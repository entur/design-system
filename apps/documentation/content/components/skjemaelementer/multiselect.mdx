---
name: Multi select
description: MultiSelect lar deg velge mellom mange valg
route: /komponenter/skjemaelementer/multi-select
parent: Komponenter
menu: Skjemaelementer
npmPackage: dropdown
tags: multi, dropdown, select, autocomplete, autosuggest, dropdown, nedtrekksmeny
---

import { Props } from 'docz';
import { MultiSelect } from '@entur/dropdown';
import { Playground } from '~/components/playground/Playground';
import { ImportStatement } from '~/components/ImportStatement';
import { CodeText } from '@entur/typography';
import PageHeader from '~/components/PageHeader';
import { FILTERS } from './dropdownItems';

<PageHeader />

<Playground>
  {() => {
    const [selectedItems, setSelectedItems] = React.useState([]);
    return (
      <>
        <div style={{ width: '15rem' }}>
          <MultiSelect
            items={FILTERS}
            onChange={e => setSelectedItems(e)}
            label="Filtrer transport"
          />
        </div>
        <div style={{ marginTop: '2rem', marginBottom: '1rem' }}>
          <CodeText>
            Valgte elementer: {selectedItems.map(item => item.label + ' ')}
          </CodeText>
        </div>
      </>
    );
  }}
</Playground>

## Komponenter

### MultiSelect

<ImportStatement imports="MultiSelect" />

<Props of={MultiSelect} />

`MultiSelect` inkludering props som er tilgjengelig via downshift.
Les om disse her https://github.com/downshift-js/downshift/tree/master/src/hooks/useSelect#advanced-props.
