---
name: Tokens
parent: Komponenter
route: /komponenter/tokens
npmPackage: tokens
---

import { colors, borderWidths, borderRadiuses, space } from '@entur/tokens';
import PageHeader from 'src/components/PageHeader';
import { TokenTable } from 'src/components/TokenTable';

<PageHeader>
  Designtokens er vårt felles designspråk satt i system - bindeleddet mellom
  alle flater.
</PageHeader>

Ved bruk av pakken _@entur/tokens_ så har man tilgang til alle gjenbrukbare verdier

som kan brukes for å designe sider og komponenter for å følge Entur sin visuelle identitet.

Disse kan aksesseres enten ved CSS, SASS, LESS eller Javscript-variabler.
Eksempelvis, importering av SASS-variablene og bruk av dem.

```css
@import '~@entur/tokens/dist/styles.scss';
.grey-text {
  color: \$colors-greys-grey40;
}
```

SASS og LESS variablene aksesseres med henholdsvis _\$_ og _@_, og er semantisk delt opp ved _-_ (bindestrek).

CSS-variablene prefikses med to bindestrek.
Javascript-variablene aksesseres som nestede objekter.

## Tokens

### Farger

<TokenTable
  tokenKey="colors"
  renderExample={value => (
    <div
      style={{
        backgroundColor: value,
        borderRadius: borderRadiuses.default,
        width: space.extraLarge,
        height: space.extraLarge,
      }}
    />
  )}
/>

### Avstander

<TokenTable
  tokenKey="space"
  renderExample={value => (
    <div
      style={{
        background: colors.brand.blue,
        borderRadius: borderRadiuses.default,
        width: value,
        height: value,
      }}
    />
  )}
/>

### Tekstvekting

<TokenTable
  tokenKey="fontWeights"
  renderExample={value => <div style={{ fontWeight: value }}>ABC abc</div>}
/>

### Tekststørrelser

<TokenTable
  tokenKey="fontSizes"
  renderExample={value => <div style={{ fontSize: value }}>ABC abc</div>}
/>

### Linjehøyder

<TokenTable tokenKey="lineHeights" />

### Breakpoints

<TokenTable tokenKey="breakpoints" />

### Border-bredde

<TokenTable
  tokenKey="borderWidths"
  renderExample={value => (
    <div
      style={{
        border: `${value}px solid ${colors.misc.black}`,
        width: space.extraLarge,
        height: space.extraLarge,
      }}
    />
  )}
/>

### Border-radius

<TokenTable
  tokenKey="borderRadiuses"
  renderExample={value => (
    <div
      style={{
        border: `${borderWidths.default}px solid ${colors.misc.black}`,
        borderRadius: value,
        width: space.extraLarge,
        height: space.extraLarge,
      }}
    />
  )}
/>

### Z-indexer

<TokenTable tokenKey="zIndexes" />
```
