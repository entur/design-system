---
name: ExpandablePanel
parent: Komponenter
route: /komponenter/layout/expandablepanel
menu: Layout og flater
npmPackage: expand
---

import { ExpandablePanel, BaseExpand, ExpandableLink } from '@entur/expand';
import { Props } from 'docz';
import { Playground } from 'src/components/Playground';
import PageHeader from 'src/components/PageHeader';
import { ImportStatement } from 'src/components/ImportStatement';

<PageHeader>
  Expandable er en rekke komponenter som brukes for å skjule innhold, som kan
  vises ved å klikke på komponenten.
</PageHeader>

<Playground>
  <ExpandablePanel title="Om Entur">
    I Norge har vi et godt kollektivtilbud. Vi tror at enda flere vil velge å
    reise kollektivt om det blir enklere å finne frem i tilbudet. Derfor samler
    vi reisedata fra alle kollektivselskap i landet.
  </ExpandablePanel>
  <ExpandablePanel title="Om Entur">
    I Norge har vi et godt kollektivtilbud. Vi tror at enda flere vil velge å
    reise kollektivt om det blir enklere å finne frem i tilbudet. Derfor samler
    vi reisedata fra alle kollektivselskap i landet.
  </ExpandablePanel>
</Playground>

## Komponenter

### ExpandablePanel

<ImportStatement imports="ExpandablePanel" />
<Props of={ExpandablePanel} />

ExpandablePanel kan brukes om man har noe innhold man vil skjule initielt, men som skal kunne åpnes og lukkes ved klikk.
ExpandablePanel skal i utgangspunktet ikke stå alene, dvs, det burde være minst _to_ ExpandablePanel ved siden av hverandre til ei hver tid.
ExpandableLink skal heller brukes om man har et alenestående stående innhold man ønsker å skjule.
Om man har lyst til å kontrollere åpningen og lukkingen selv, så kan man benytte seg av `open` og `onToggle`.
Se eksempel på dette under.

<Playground>
  {() => {
    const [open, setOpen] = React.useState(false);
    return (
      <ExpandablePanel
        title="Om Entur"
        open={open}
        onToggle={() => setOpen(!open)}
      >
        I Norge har vi et godt kollektivtilbud. Vi tror at enda flere vil velge
        å reise kollektivt om det blir enklere å finne frem i tilbudet. Derfor
        samler vi reisedata fra alle kollektivselskap i landet.
      </ExpandablePanel>
    );
  }}
</Playground>

### Expandable Link

ExpandableLink brukes om man ønsker å skjule et innhold, og i motsetning til ExpandablePanel, så står den gjerne alene uten andre "Expand"-komponenter.

<Playground>
  <ExpandableLink title="Se mer info">
    Her står det mye mer info, under en mindre knapp som ikke tar så mye fokus
    fra brukeren, men fortsatt har muligheten til åpning/lukking. Her står det
    mye mer info, under en mindre knapp som ikke tar så mye fokus fra brukeren,
    men fortsatt har muligheten til åpning/lukking.
  </ExpandableLink>
</Playground>

### BaseExpand

<ImportStatement imports="BaseExpand" />
<Props of={BaseExpand} />

ExpandablePanel (og andre komponenter i designsystemet) benytter seg av `<BaseExpand>`, så om man har behov for å benytte seg av åpne/lukkemekanismen direkte, kan man benytte seg av denne.
