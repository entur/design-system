---
name: Chips
route: /komponenter/chips
parent: Komponenter
npmPackage: chip
---

import { Props } from 'docz';
import { ActionChip, ChoiceChip, ChoiceChipsGroup } from '@entur/chip';
import { Playground } from '~/components/Playground';
import { ImportStatement } from '~/components/ImportStatement';
import PageHeader from '~/components/PageHeader';

<PageHeader> Chips er en komponent for å gjøre mindre handlinger, relatert til hovedflyten. </PageHeader>

<Playground>
  {() => {  
    const [value, setValue] = React.useState(null);
    return(
      <>
        <ChoiceChipsGroup
          name="city"
          label="Velg din hjemby"
          onChange={e => setValue(e.target.value)}
          value={value}
        >
          <ChoiceChip value="Oslo">Oslo</ChoiceChip>
          <ChoiceChip value="Bergen">Bergen</ChoiceChip>
          <ChoiceChip value="Leknes">Leknes</ChoiceChip>
        </ChoiceChipsGroup>
        <br/>
        <br/>
        <ActionChip>Klikk meg</ActionChip> 
      <>
    );
  }}
</Playground>

## Komponenter

### ActionChip

<ImportStatement imports="ActionChip" />
<Props of={ActionChip} />

