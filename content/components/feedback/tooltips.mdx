---
name: Tooltip
description: Tooltip kan brukes for å gi ekstra informasjon til brukeren ved hover eller fokus.
route: /komponenter/feedback/tooltip
parent: Komponenter
menu: Feedback
npmPackage: tooltip
tags: hjelpetekst, tooltip, verktøyhjelp, popover
---

import { Tooltip } from '@entur/tooltip';
import { PrimaryButton } from '@entur/button';
import { Props } from 'docz';
import { Playground } from '~/components/Playground';
import PageHeader from '~/components/PageHeader';
import { ImportStatement } from '~/components/ImportStatement';

<PageHeader />

<Playground>
  <Tooltip content="Her er tooltipen" placement="top">
    <span>Denne teksten har en tooltip</span>
  </Tooltip>
  <br />
  <br />
  <br />
  <Tooltip content="Her er tooltipen" placement="bottom-right" isOpen>
    <span>Denne teksten har en tooltip</span>
  </Tooltip>
</Playground>

## Komponenter

### Tooltip

<ImportStatement imports="Tooltip" />
<Props of={Tooltip} />

Du wrapper `<Tooltip />` rundt elementet du ønsker at skal ha en tooltip.
**Obs:** Plasseringen av tooltip baserer seg på `ref`,
så pass derfor på at komponenten som wrappes støtter `forwardRef`. Les mer om dette [her](https://reactjs.org/docs/forwarding-refs.html).

Om man sender med `isOpen` så får Tooltip en lukkeknapp for å håndtere situasjoner hvor man ønsker kontrollere visningen av Tooltip.

## Retningslinjer

_Unngå_ å legge nødvendig og kritisk informasjon i tooltip'en.

Ettersom tooltip _helst skal_ brukes på fokuserbare elementer, så vises også tooltip om knappen/linken etc er fokusert

<Playground>
  <Tooltip content="Tooltip til knappen" placement="top">
    <PrimaryButton>Knapp med tooltip</PrimaryButton>
  </Tooltip>
</Playground>
