---
name: Loaders
route: /komponenter/feedback/loaders
parent: Komponenter
menu: Feedback
npmPackage: loader
tags: loader, spinner, wait, vent, indikator, opplasting, last opp
---

import { Props } from 'docz';
import { Playground } from '~/components/Playground';
import PageHeader from '~/components/PageHeader';
import InlineComponent from '~/components/InlineComponent';

import { ImportStatement } from '~/components/ImportStatement';

import { Button } from '@entur/button';
import { Loader } from '@entur/loader';

<PageHeader>
  Loaders brukes for å indikere at en prosess er i gang, og eventuelt hvor langt
  denne prosessen har kommet.
</PageHeader>

<Playground>
  {() => {
    const [progress, setProgress] = React.useState(0);
    return (
      <>
        <Loader>Vennligst vent</Loader>
        <Loader progress={20}>Laster opp fil</Loader>
        <Loader progress={progress}>
          <Button
            onClick={() => setProgress(prev => (prev + 10) % 101)}
            variant="secondary"
          >
            Gå videre &rarr;
          </Button>
        </Loader>
      </>
    );
  }}
</Playground>

## Komponenter

### Loader

<ImportStatement imports="Loader" />
<Props of={Loader} />

Loader-komponenten kan brukes på to forskjellige måter - til filopplastning og til å vise at "noe skjer".

Ved filopplastning kan du sende inn et tall til `progress`-propen - du vil da få en ramme rundt loading-indikatoren.

Hvis du ikke spesifiserer en `progress`-prop, defaulter den til "indeterminate" - det vil si at den viser en vente-animasjon for å vise brukeren at siden er opptatt med å hente data, kalkulere priser osv.

Du kan sende inn en tekstlig beskrivelse som `children`, som gir brukeren en indikasjon på hva som skjer. Dette bør være i størrelsesorden 1-2 ord.
