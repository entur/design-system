---
name: Timepicker
route: /komponenter/skjemaelementer/timepicker
parent: Komponenter
menu: Skjemaelementer
npmPackage: datepicker
tags: timepicker, tidvelger, tid
---

import { Props } from 'docz';
import { TimePicker } from '@entur/datepicker';
import { Playground } from '~/components/Playground';
import { ImportStatement } from '~/components/ImportStatement';
import { addWeeks } from 'date-fns';
import PageHeader from '~/components/PageHeader';
import { addDays } from 'date-fns';

<PageHeader>Timepicker er en komponent for å velge et tidspunkt.</PageHeader>

<Playground>
  {() => {
    const [date, setDate] = React.useState();
    return (
      <TimePicker
        style={{ width: '10rem' }}
        selectedTime={date}
        onChange={date => setDate(date)}
      />
    );
  }}
</Playground>

## Komponenter

### TimePicker

<ImportStatement imports="TimePicker" />
<Props of={TimePicker} />

### NativeTimePicker

<ImportStatement imports="TimePicker" />
<Props of={TimePicker} />

Som med datepicker, så anbefales det å bruke NativeTimePicker dersom du skal vise en timepicker for mobile enheter.
