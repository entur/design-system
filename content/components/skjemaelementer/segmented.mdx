---
name: Segmented Control
description: SegmentedControl er alternative komponenter for å gi radio- og checkbox-funksjonalitet.
route: /komponenter/skjemaelementer/segmented-control
parent: Komponenter
menu: Skjemaelementer
npmPackage: form
tags: radio, checkbox
---

import { Props } from 'docz';
import { SegmentedGroup, SegmentedControl } from '@entur/form';
import { Playground } from '~/components/Playground';
import { ImportStatement } from '~/components/ImportStatement';
import { BoldIcon, ItalicIcon, UnderlineIcon } from '@entur/icons';
import PageHeader from '~/components/PageHeader';

<PageHeader />

<Playground>
  {() => {
    const [value, setValue] = React.useState(null);
    const [value1, setValue1] = React.useState(null);
    const [checkbox1, setCheckbox1] = React.useState(false);
    return (
      <div>
        <div style={{ marginBottom: '1rem' }}>
          <SegmentedGroup
            name="city"
            label="Velg din hjemby"
            onChange={e => setValue(e.target.value)}
            value={value}
            multiple={false}
          >
            <SegmentedControl value="Oslo">Oslo</SegmentedControl>
            <SegmentedControl value="Bergen">Bergen</SegmentedControl>
            <SegmentedControl value="Leknes">Leknes</SegmentedControl>
          </SegmentedGroup>
        </div>
        <div style={{ marginBottom: '1rem' }}>
          <SegmentedGroup
            label="Velg skrifttype"
            value={value1}
            multiple={true}
            onChange={e => console.log(e.target.name, e.target.checked)}
            style={{ width: '7rem' }}
          >
            <SegmentedControl value="b" name="b">
              <BoldIcon inline />
            </SegmentedControl>
            <SegmentedControl value="k" name="k">
              <ItalicIcon inline />
            </SegmentedControl>
            <SegmentedControl value="u" name="u">
              <UnderlineIcon inline />
            </SegmentedControl>
          </SegmentedGroup>
        </div>
        <div style={{ display: 'flex' }}>

        </div>
      </div>
    );

}}

</Playground>

## Komponenter

### SegmentedGroup

<ImportStatement imports="SegmentedGroup" />
<Props of={SegmentedGroup} />

Om man wrapper `SegmentedControl` i en `SegmentedGroup`, så kan man ved hjelp av `multiple` bestemme om man ønsker seg radio- eller checkbox-funksjonalitet.
Multiple i dette tilfelle refererer til om man har mulighet til å velge flere alternativer, det vil si, flere valg betyr Checkbox.

### SegmentedControl

<ImportStatement imports="SegmentedControl" />
<Props of={SegmentedControl} />
