---
title: Farger
description: Fargepaletten er et viktig grafisk virkemiddel i identiteten vår. Det er
  viktig at fargene brukes riktig og med omhu, for å oppnå et gjenkjennelig
  uttrykk.
route: /identitet/verktoykassen/farger
parent: Identitet
menu: Verktøykassen
order: 4
---

import { graphql } from 'gatsby'
import { borderWidths, colors, space } from '@entur/tokens';
import Colors from '../../../components/Colors/Colors';
import { SEO } from '../../../components/seo/SEO';

export const Head = ( props ) => {
  return <SEO title={props.pageContext.frontmatter.title} description={props.pageContext.frontmatter.description} pathname={props.pageContext.frontmatter.route} />
}

export const query = graphql`
query fargerQuery {
  files: allFile(filter: {sourceInstanceName: {eq: "media"}, relativeDirectory:  {glob: "images/identitet/verktoykassen/farger"}, extension: {eq: "png"}}) {
    images: nodes {
      name
       childImageSharp {
         gatsbyImageData(
          layout: CONSTRAINED
        )
      }
    }
  }
}
`;

<PageHeader/>


## Profilfarger

Når det er snakk om kjernekommunikasjon, skal vi prøve å forholde oss til disse hovedfargene.

<Colors colorType="brand"></Colors>

## Funksjonsfarger

Disse fargene er ikke direkte knyttet til identiteten til Entur,
men brukes til spesifikke funksjoner i grensesnittet som varselmeldinger, statusmeldinger eller validering i skjemaelementer.
Disse fargene er ikke ment som dekorasjon, og skal kun brukes til å formidle et budskap.

<Colors colorType="validation" />

## Blåtoner

Disse blå fargenyansene brukes hovedsakelig som bakgrunnsfarger for
Contrast seksjoner og enkelte elementer i designet som for eksempel
borders og dividers.

<Colors colorType="blues" />

## Gråtoner

Gråtonene symboliserer det tekniske og robuste i plattformen vår. Brukes
hovedsakelig som bakgrunnsfarger for å få frem det lyse og luftige ved
layouten i designet.

<Colors colorType="greys" />

## Transportfarger

De ulike transportmidlene har hver sin representative farge for å skille
de fra hverandre. Vanngående transport har for eksempel alltid lyseblå
farge og er derfor også markert med den fargen på ikoner (ferje,
bilferje, cruise osv.) og på reiselinjer. Transportmiddelfargene finnes
også for Contrast seksjoner.

<Colors colorType="transport" />

## Farger for datavisualisering

Vi har også en egen fargepalett som skal benyttes ved visualisering av data, i forskjellige typer grafer og andre visualiseringer av informasjon.
[Les mer om farger for datavisualisering her.](/identitet/verktoykassen/datavisualisering)

<Colors colorType="data" />

## Retningslinjer for bruk

### Vekting av profilfarger

Hvordan fargene vektes er en viktig del av å skape det ønskede uttrykket for Entur. Hovedfargene er blå, hvit og rød, og disse burde alltid være med. Korallfargen er vektet lavt, men er viktig og skal alltid være tilstede i uttak. Den brukes ofte til detaljer, til tider så detaljert som bare i logo.

<ImageDisplay
  imgSource={
    props.data.files.images.find(image => image.name === 'ColorBalance')
      ?.childImageSharp.gatsbyImageData
  }
  style={{
    padding: '0.25rem',
    border: `2px solid ${colors.greys.grey70}`,
    marginBottom: space.extraLarge3,
    display: 'flex',
  }}
  alt="Viser vektleggingen av profilfargene, Blue og white er størst, så lavender, coral og til slutt peach"
  preset="full-width-image"
/>

### Prinsipper

<DoDontGroup>
  <DoDontCard 
    variant="negative"
    imgSource={props.data.files.images.find(image => image.name === "Dont1")?.childImageSharp.gatsbyImageData}
    alt="" 
    noPadding
  >
    Unngå mørk tekst på blå bakgrunn
  </DoDontCard>
  <DoDontCard 
    variant="negative" 
    imgSource={props.data.files.images.find(image => image.name === "Dont2")?.childImageSharp.gatsbyImageData}
    alt="" 
    noPadding
  >
    Unngå korallrød bakgrunn
  </DoDontCard>
</DoDontGroup>
<DoDontGroup>
  <DoDontCard 
    variant="negative" 
    imgSource={props.data.files.images.find(image => image.name === "Dont3")?.childImageSharp.gatsbyImageData}
    alt="" 
    noPadding
  >
    Unngå identitetløst uttryk
  </DoDontCard>
  <DoDontCard 
    variant="negative" 
    imgSource={props.data.files.images.find(image => image.name === "Dont4")?.childImageSharp.gatsbyImageData}
    alt="" 
    noPadding
  >
    Unngå lys tekst på lys bakgrunn
  </DoDontCard>
</DoDontGroup>

Les mer om kontrastkrav for farger på [Universell utforming](/universell-utforming) siden og bruk vår [Color Contrast Checker](/universell-utforming/kontrast-sjekker) for å hjelpe deg med å bestemme lesbarheten til tekst og kontrasten til grafiske objekter.
