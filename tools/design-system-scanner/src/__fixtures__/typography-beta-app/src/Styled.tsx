import styled from 'styled-components';

// Reaches past the component API from a CSS-in-JS literal
export const Wrapper = styled.div`
  .eds-text--paragraph {
    color: rebeccapurple;
  }

  .eds-heading {
    letter-spacing: 0.02em;
  }
`;
