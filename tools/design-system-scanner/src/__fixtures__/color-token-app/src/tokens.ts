import { colors } from '@entur/tokens';

// Member access resolves to the individual token, not just "colors was imported"
export const brand = colors.brand.coral;
export const blue = colors.blues.blue50;
export const alsoBlue = colors.blues.blue50;
